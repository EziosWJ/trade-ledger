# AGENTS.MD

## 1. 先理解，再编码

修改前先理解需求、相关代码和现有约束。

不要基于猜测修改代码，也不要因为看到一种“更好的写法”就立即重构现有实现。

对于低风险、可逆且符合现有项目惯例的小歧义，可以做合理判断并继续。

如果涉及架构调整、数据迁移、公共接口、兼容性、不可逆操作，或者存在多个影响明显不同的实现方案，应先询问用户。

> 小问题自主判断，大决策先确认。

## 2. 优先简单方案

实现能够完整满足当前需求的最简单方案。

不要主动增加：

* 用户没有要求的功能；
* 为假想未来准备的扩展点；
* 没有实际需要的抽象层；
* 不必要的配置项、接口或设计模式。

不要为了理论上的“优雅”增加实际复杂度。

## 3. 保持修改范围最小

只修改完成当前任务真正需要修改的内容。

不要顺手：

* 重构无关代码；
* 修改无关命名；
* 格式化无关文件；
* 删除无关死代码；
* 修复当前任务之外的问题；
* 更换现有技术方案或依赖。

每一处修改都应该能够明确说明它与当前需求的关系。

发现无关问题可以指出，但不要未经要求直接修改。

## 4. 尊重现有代码库

优先遵循项目已有的：

* 架构和目录结构；
* 命名和编码风格；
* 错误处理和日志方式；
* 数据访问方式；
* 已使用的框架和依赖；
* 测试和构建方式。

除非现有方案确实阻碍需求，否则不要为了个人偏好替换它。

## 5. 不隐藏问题

不要为了让任务看起来完成而：

* 吞掉异常；
* 伪造数据；
* 写死本应动态获取的数据；
* 跳过必要校验；
* 删除或降低测试标准；
* 用临时 workaround 冒充正式解决方案。

无法可靠解决的问题，应明确说明原因和影响。

## 6. 验证结果

代码写完不代表任务完成。

修改后，根据项目实际情况选择合适的验证方式，例如：

* 测试；
* lint；
* typecheck；
* 编译或 build；
* 启动运行；
* API 调用；
* 实际复现场景。

Bug 修复应尽量验证原问题确实消失。

验证的目标是证明需求已经正确实现，而不是机械地增加测试。

---

# 核心原则

**Think Before Coding**
先理解问题和现有实现，再动手。

**Simplicity First**
选择满足当前需求的最简单方案。

**Surgical Changes**
只修改必要内容，不扩大任务范围。

**Verify the Outcome**
以结果经过验证作为完成标准，而不是以代码已经生成作为完成标准。

> 用最小必要改动，准确解决当前问题，并验证结果。
