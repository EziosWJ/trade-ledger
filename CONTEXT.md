# Trade Ledger

面向电脑城 / 维修装机 / IT 渠道商家的生意往来账，记录“生意发生了什么”并跟踪应收应付到结清。

## Language

### 往来对象

**往来对象 (Counterparty)**:
与本商家发生业务的客户、供应商、同行或个人，类型为非互斥标签，可同时持有应收和应付。
_Avoid_: 客户、用户、账号

**对象标签 (CounterpartyTag)**:
往来对象的身份标签，取值为 `customer` / `supplier` / `peer` / `individual`，可多选。
_Avoid_: 对象类型（单选含义）

### 业务

**业务往来 (Trade)**:
一笔形成应收或应付的原始业务，金额方向创建即锁定，只能结算或冲销，不能原地修改。
_Avoid_: 订单、交易、账单

**业务类型 (TradeType)**:
业务的种类，取值为 `sale` / `service` / `purchase` / `transfer`，其中 `transfer` 合并调货与串货并以方向区分。
_Avoid_: 调货、串货（作为独立类型）

**明细 (TradeItem)**:
业务下可选的商品 / 服务行，可空；为空时业务为一口价总额。
_Avoid_: 商品表、库存

### 结算

**应收 (Receivable)**:
由 `sale` / `service` / 转入方向 `transfer` 形成的待收金额。
_Avoid_: 收入、营收

**应付 (Payable)**:
由 `purchase` / 转出方向 `transfer` 形成的待付金额。
_Avoid_: 支出、成本

**结算 (Settlement)**:
针对一笔业务的后续收款 / 付款记录，一笔业务可多次结算，累计金额达到业务金额即结清。
_Avoid_: 回款（仅指收款时）、核销（含义过载）

**收款付款 (Payment)**:
V1 的现金结算种类，现金收 / 付，多次累加，禁止超收超付。
_Avoid_: 转账（与 transfer 混淆）

**抵扣 (Offset)**:
同一往来对象的应收冲应付，双边各记一笔并共享同一 `offset_id`，V1 支持部分或全部抵扣。
_Avoid_: 对冲、抹账

**调整 (Adjustment)**:
优惠 / 抹零 / 坏账等非现金结清，单边核销，第二迭代实现。
_Avoid_: 优惠（作为通用结算词）

### 视图

**余额 (Balance)**:
业务累计减结算累计的剩余金额，每笔余额可追溯到原始业务及后续结算。
_Avoid_: 欠款（方向不明）

**时间线 (Timeline)**:
同一往来对象下业务与结算按时间倒序混排的完整流水。
_Avoid_: 账单列表、对账单
